/* Class description */
class A {
	
	// Method declarations

	int methodInt(int x) {

		int y;

		// If statements with expressions
		if (x==y) {
			// Do something
		}

		if (A.getValue()) {
			// Do something
		}

		if (!B) {
			// Do something
		}
		return y;
	}

	int max(int x,int y) {
		if (x>=y) {
			return x; 
		} else {
			return y;
		}
	}

	int method(int x,int y,int z) {
		return (x*x)+(y*y)+(z*z);
	}

}